{
  "name": "@blastz/inject-config",
  "version": "0.1.1",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "tsc": "tsc",
    "test": "jest",
    "format": "prettier --ignore-path .gitignore --write .",
    "push": "npm run format && npm run tsc && npm run test && npm publish --access public --registry=https://registry.npmjs.org"
  },
  "author": "blastz",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "prettier": "^2.3.2"
  },
  "dependencies": {
    "@types/node": "^16.0.1",
    "deepmerge": "^4.2.2"
  }
}
